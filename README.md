# Discord Scheduler Bot 📅

بوت ديسكورد لجمع الرسائل والصور وإرسالها بتوقيت مجدول.

## المميزات ✨

- **إضافة رسائل**: إرسال نصوص وصور للبوت عبر الرسائل الخاصة أو الأوامر
- **إدارة الرسائل**: عرض، تعديل، وحذف الرسائل قبل نشرها
- **جدولة النشر**: تحديد وقت يومي لنشر جميع الرسائل تلقائياً
- **الثريدات**: إنشاء مجموعات رسائل تُنشر كثريد في ديسكورد
- **النشر اليدوي**: نشر جميع الرسائل فوراً عند الحاجة

## التثبيت 🛠️

### 1. المتطلبات
- Node.js v16.9.0 أو أحدث
- حساب ديسكورد للمطورين

### 2. إنشاء بوت ديسكورد
1. اذهب إلى [Discord Developer Portal](https://discord.com/developers/applications)
2. أنشئ تطبيق جديد (New Application)
3. اذهب إلى قسم "Bot" وأنشئ بوت
4. فعّل الخيارات التالية في "Privileged Gateway Intents":
   - MESSAGE CONTENT INTENT
5. انسخ التوكن (Token)
6. اذهب إلى "OAuth2" > "URL Generator"
7. اختر الصلاحيات:
   - Scopes: `bot`, `applications.commands`
   - Bot Permissions: `Send Messages`, `Create Public Threads`, `Attach Files`, `Read Message History`
8. انسخ الرابط وافتحه لإضافة البوت لسيرفرك

### 3. إعداد المشروع

```bash
# استنساخ المشروع
cd discord-scheduler-bot

# تثبيت المكتبات
npm install

# إنشاء ملف البيئة
copy .env.example .env
```

### 4. إعداد ملف .env

```env
DISCORD_TOKEN=توكن_البوت_هنا
CLIENT_ID=معرف_التطبيق_هنا
GUILD_ID=معرف_السيرفر_هنا
PUBLISH_CHANNEL_ID=معرف_قناة_النشر_هنا
```

**للحصول على المعرفات:**
- `CLIENT_ID`: من صفحة التطبيق في Developer Portal
- `GUILD_ID`: كليك يمين على السيرفر > Copy Server ID
- `PUBLISH_CHANNEL_ID`: كليك يمين على القناة > Copy Channel ID

> ملاحظة: يجب تفعيل "Developer Mode" في إعدادات ديسكورد

### 5. تشغيل البوت

```bash
npm start
```

أو للتطوير:
```bash
npm run dev
```

## الأوامر 📝

### إضافة رسالة
```
/add [content] [image] [time]
```
- `content`: النص (اختياري)
- `image`: صورة مرفقة (اختياري)
- `time`: وقت النشر بصيغة `YYYY-MM-DD HH:MM` (اختياري)

**أو أرسل رسالة مباشرة للبوت في الخاص!**

### عرض الرسائل
```
/list
```
يعرض جميع رسائلك المعلقة

### تعديل رسالة
```
/edit <id> [content] [time]
```
- `id`: رقم الرسالة (مطلوب)
- `content`: المحتوى الجديد (اختياري)
- `time`: الوقت الجديد (اختياري)

### حذف رسالة
```
/delete <id>
```

### إدارة الثريدات
```
/thread create <name> [time]     # إنشاء ثريد جديد
/thread add <thread_id> [content] [image]  # إضافة رسالة للثريد
/thread list                      # عرض الثريدات
/thread view <thread_id>          # عرض رسائل ثريد
/thread edit <thread_id> [name] [time]     # تعديل ثريد
/thread delete <thread_id>        # حذف ثريد
```

### الجدولة (للمشرفين)
```
/schedule set <time>    # تعيين وقت النشر اليومي (HH:MM)
/schedule status        # عرض حالة الجدولة
/schedule stop          # إيقاف الجدولة
```

### النشر اليدوي (للمشرفين)
```
/publish
```
ينشر جميع الرسائل المعلقة فوراً

## طريقة العمل 🔄

1. **إضافة المحتوى**: أرسل رسائل وصور للبوت عبر:
   - الرسائل الخاصة (DM)
   - أمر `/add`

2. **إدارة المحتوى**: استخدم `/list` و `/edit` و `/delete`

3. **جدولة النشر**: استخدم `/schedule set 14:00` لتحديد وقت النشر

4. **النشر**: في الوقت المحدد، ينشر البوت جميع الرسائل في القناة المحددة

## الثريدات 🧵

لإنشاء سلسلة رسائل (Thread):

1. أنشئ مجموعة: `/thread create "عنوان الثريد" 2024-12-20 14:00`
2. أضف رسائل: `/thread add 1 "الرسالة الأولى"`
3. أضف المزيد: `/thread add 1 "الرسالة الثانية" [صورة]`

عند النشر:
- الرسالة الأولى تُنشر في القناة
- يُنشأ ثريد بالعنوان المحدد
- باقي الرسائل تُنشر داخل الثريد

## الدعم 💬

إذا واجهت مشاكل:
1. تأكد من صحة التوكن والمعرفات
2. تأكد من صلاحيات البوت في القناة
3. تحقق من تفعيل MESSAGE CONTENT INTENT

## الترخيص 📄

MIT License
